use crate::problem::Solver;

pub struct DayX;

#[allow(dead_code, unused_variables)]
impl DayX {
    fn parse(&self, input: &str) -> &str {
        "Parser not implemented yet!"
    }
}

#[allow(dead_code, unused_variables)]
impl Solver for DayX {
    fn part_one(&self, input: &str) -> String {
        "Part one not solved yet!".to_string()
    }

    fn part_two(&self, input: &str) -> String {
        "Part two not solved yet!".to_string()
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn part_one_works() {
        assert_eq!(1 + 1, 2);
    }

    #[test]
    fn part_two_works() {
        assert_eq!(1 + 1, 2);
    }
}
